# Brand assets

`bdgrz-mark.png` is the transparent 256-pixel web mark used by the application
shell. `bdgrz-mark-128.png` is the square favicon derivative. Both preserve the
supplied black-and-white badger artwork and contain a real alpha channel.

Keep these public filenames stable when a canonical vector source becomes
available so the shell and document metadata do not need to change.
